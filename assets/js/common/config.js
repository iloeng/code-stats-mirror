// Common configuration values

/**
 * Locale of the system for Intl APIs
 */
const LOCALE = 'en-US';

/** Breakpoint of small size (sm) in px. */
const SMALL_BREAKPOINT = 480;

/** Breakpoint of medium size (md) in px. */
const MEDIUM_BREAKPOINT = 768;

/** Breakpoint of large size (lg) in px. */
const LARGE_BREAKPOINT = 960;

/** Breakpoing of extra large size (xl) in px. */
const EXTRA_LARGE_BREAKPOINT = 1100;

export {
  LOCALE,
  SMALL_BREAKPOINT,
  MEDIUM_BREAKPOINT,
  LARGE_BREAKPOINT,
  EXTRA_LARGE_BREAKPOINT,
};
