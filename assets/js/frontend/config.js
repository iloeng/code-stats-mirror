// Common configuration values

/**
 * Normal background color in the UI.
 */
const BACKGROUND_COLOR = '#ffffff';

/**
 * Normal text color in the UI.
 */
const TEXT_COLOR = '#000000';

/**
 * Background accent color in the UI.
 */
const ACCENT_COLOR = '#3e4053';

/**
 * Color of text when on accent background.
 */
const ACCENT_TEXT = '#fcfcfe';

/**
 * How many percent of the way from bg color to the accent color until text color is flipped to `ACCENT_TEXT`.
 */
const ACCENT_TEXT_FLIP_PERCENT = 50;

/**
 * How many hours back in time are pulses considered recent?
 */
const RECENT_HOURS = 12;

export {
  BACKGROUND_COLOR,
  TEXT_COLOR,
  ACCENT_COLOR,
  ACCENT_TEXT,
  ACCENT_TEXT_FLIP_PERCENT,
  RECENT_HOURS,
};
